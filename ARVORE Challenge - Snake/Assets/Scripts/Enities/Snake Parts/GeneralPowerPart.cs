using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/// <summary>
/// Activates when has contact with something regardless of the position in the snake.
/// </summary>
public class GeneralPowerPart : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
